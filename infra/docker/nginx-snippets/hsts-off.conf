# Default HSTS state: no header. A self-signed cert is not CA-trusted, so
# pinning a browser to HTTPS-only for this host (what HSTS does) leaves no
# easy way back if the cert setup breaks — the browser refuses the plain-HTTP
# fallback too. Swap in hsts-on.conf only once a CA-trusted cert is in place.
